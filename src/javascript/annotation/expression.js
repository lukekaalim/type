// @flow strict

/*::
type IdentifierExpression = {
  type: 'identifier',
  identifier: string,
};

type ValueExpression = {
  type: 'value',
  literal: mixed,
};

type Expression =
  | FunctionExpression
  | IdentifierExpression
  | ValueExpression

type FunctionExpression = {
  type: 'function',

  parameters: Expression[],
  returns: null | Expression,
  throws: null | Expression,
};
*/

const parseExpression = (expressionText) => {
  
};