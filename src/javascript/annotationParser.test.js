// @flow strict
import test from '@lukekaalim/test';

const expectAnnotationParser = () => test.assert('createAnnotationFromComment() should turn acorn comment objects into annotation', () => {

});

export {
  expectAnnotationParser,
};
