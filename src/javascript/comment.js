// @flow strict

/*::
type Comment = {
  isBlock: boolean,
  content: string,
  start: number,
  end: number,
};
*/

const createCommentCollector => () => {
  const comments = [];

  const onComment = () => {

  };
};